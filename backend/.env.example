# =============================================================================
# CONFIGURACIÓN DE DESARROLLO LOCAL
# =============================================================================
# Copia este archivo a .env y ajusta los valores según tu entorno
# 
# IMPORTANTE: El .env debe apuntar a la BD LOCAL (Docker), NO a producción
# Para sincronizar datos de producción, usa: ./scripts/db-sync.sh
# =============================================================================

# Entorno
NODE_ENV=development
PORT=5000

# Base de Datos LOCAL (Docker en puerto 5433)
# ⚠️ NO usar la BD de producción aquí
DB_HOST=localhost
DB_PORT=5433
DB_NAME=erp_db
DB_USER=erp_user
DB_PASSWORD=erp_password_dev_2024

# JWT (usar una clave diferente a producción)
JWT_SECRET=erp-jwt-secret-development-local-key
JWT_EXPIRE=8h

# Logging
LOG_LEVEL=debug

# =============================================================================
# NUNCA COMMITEAR EL ARCHIVO .env CON CREDENCIALES REALES
# =============================================================================
